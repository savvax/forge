openapi: 3.0.3
info: { title: External Ref, version: '1.0' }
paths:
  /payouts:
    post:
      operationId: createPayout
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: 'other.yaml#/components/schemas/PayoutRequest'
      responses:
        '201': { description: Created }
